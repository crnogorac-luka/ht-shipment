class Location {
    constructor(id, href, name, geometryType, accuracy, spatialRef, geometry) {
      this.id = id;
      this.href = href;
      this.name = name;
      this.geometryType = geometryType;
      this.accuracy = accuracy;
      this.spatialRef = spatialRef;
      this.geometry = geometry;
    }
  }
  