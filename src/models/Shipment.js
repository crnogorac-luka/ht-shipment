class Shipment {
    constructor(id, customerId, createdDate, carrier, location, status) {
      this.id = id;
      this.customerId = customerId;
      this.createdDate = createdDate;
      this.carrier = carrier;
      this.location = location;
      this.status = status;
    }
  }