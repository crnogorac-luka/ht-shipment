class AddressRoleReference {
    constructor(id, href, role, name, validFor) {
      this.id = id;
      this.href = href;
      this.role = role;
      this.name = name;
      this.validFor = validFor;
    }
  }