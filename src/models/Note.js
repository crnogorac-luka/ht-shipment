class Note {
    constructor(id, author, date, text, validFor) {
      this.id = id;
      this.author = author;
      this.date = date;
      this.text = text;
      this.validFor = validFor;
    }
  }