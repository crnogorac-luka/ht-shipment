class SubAddress {
    constructor(id, href, type, addressRole, validFor, note, subAddressSpecRef) {
      this.id = id;
      this.href = href;
      this.type = type;
      this.addressRole = addressRole;
      this.validFor = validFor;
      this.note = note;
      this.subAddressSpecRef = subAddressSpecRef;
    }
  }